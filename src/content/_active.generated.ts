// AUTO-GENERATED default. The demo-gen pipeline OVERWRITES this file per lead
// via `npm run gen` (scripts/gen-site.mjs) inside its cloned copy — that write
// never lands on main. Committed default keeps the repo buildable standalone.
import type { SiteContent, ThemeName } from "./types";
import { brandonElectric } from "./brandon-electric";

export const site: SiteContent = brandonElectric;
export const theme: ThemeName = "electric";
